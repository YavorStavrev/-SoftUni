function RequiredReading(pages, hour, days){
    let totalTime = pages / hour;
    let hoursPerDay = totalTime / days;
    console.log(hoursPerDay);
}
RequiredReading(212,

    20 ,
    
    2);